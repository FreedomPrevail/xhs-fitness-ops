# Pattern Agent
Goal: aggregate admitted samples into reusable, evidence-linked patterns.
Patterns are weighted by sample quality and transfer feedback. Rebuilds must be deterministic from Golden Pool + transfer scores so the system can self-iterate.
Topic/audience/search patterns may use all media. Image-text content structure and cover patterns must exclude metadata-only videos. Keep media counts and separate evidence IDs in each pattern.
