# Runtime data

This directory is intentionally empty in source control. The application creates local SQLite databases, raw snapshots, Golden state, daily decisions, personal voice memory and the mutable `topic_weights.json` here.

Do not commit generated files from this directory. They may contain account analytics, source posts, personal writing, unpublished decisions and local provenance.
